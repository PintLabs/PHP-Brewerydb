This service is a simple PHP wrapper for the Brewerydb.com API.


Usage:

$apiKey = ''; // your Brewerydb api key
$brewerydb = new Bn_Service_Brewerydb($apiKey);

try {
    $results = $brewerydb->getBreweries();
} catch (Exception $e) {
    $throw $e;
}
